package com.querifylabs.blog.optimizer.metadata;

import com.blazingdb.calcite.metastore.metadata.TupleMetadata;

public class SchemaExprParser {
    public static TupleMetadata parseSchema(String schemaString) {
        TupleMetadata expectedSchema = null;
        //@todo, @alex
        //= new SchemaBuilder().buildSchema();
        return    expectedSchema;
    }
}
